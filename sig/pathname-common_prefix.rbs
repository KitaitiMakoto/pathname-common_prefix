# TypeProf 0.21.8

# Classes
class Pathname
  def self.common_prefix: (*Array[Array[pathnamable] | pathnamable] | pathnamable paths) -> Pathname?
  def common_prefix: (*Array[Array[pathnamable] | pathnamable] others) -> Pathname?
  def start_with?: (pathnamable other) -> bool
end

type pathnamable = Pathname | _ToStr

# Class just for type checking
class NonEmptyArray[T] < Array[T]
  def pop: -> T
end
